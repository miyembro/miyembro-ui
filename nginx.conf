events {}
http {
    server {
        listen 80;  # Traefik handles HTTPS externally
        server_name _;  # Accept all hosts (Traefik routes traffic)

        root /usr/share/nginx/html;
        index index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}